#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,x;
    cin>>x;

    a=ceil(sqrt(x));
    b=floor(sqrt(x));

    char str[3];
    str[0]='a';
    str[1]='b';
    str[2]='c';
    puts(str);
}
